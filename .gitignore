# pair-factory runtime state — never committed
.pair/

# per-machine agent overrides
.claude/settings.local.json

# OS noise
.DS_Store
Thumbs.db

# stack ignores (added by rung 1 — kit finding: foundation rungs must do this)
node_modules/
dist/
